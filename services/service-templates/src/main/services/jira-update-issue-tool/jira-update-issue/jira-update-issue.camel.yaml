- route:
    id: jira-update-issue
    from:
      uri: ai-tool:jira-update-issue
      parameters:
        tags: jira
        description: "{{tool.description}}"
        parameter.wanaku_body: string
        parameter.wanaku_body.description: "The issue fields to update"
      steps:
        - to:
            uri: jira:updateIssue
            parameters:
              jiraUrl: "{{jira.url}}"
              accessToken: "{{jira.access.token}}"
