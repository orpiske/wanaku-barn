- route:
    id: qdrant-similarity-search
    from:
      uri: ai-tool:qdrant-similarity-search
      parameters:
        tags: qdrant
        description: "{{tool.description}}"
        parameter.wanaku_body: string
        parameter.wanaku_body.description: "The search query or vector data for similarity search"
      steps:
        - setHeader:
            name: CamelQdrantAction
            constant: "SIMILARITY_SEARCH"
        - setHeader:
            name: CamelQdrantWithPayload
            constant: "true"
        - to:
            uri: "qdrant:{{qdrant.collection}}"
            parameters:
              host: "{{qdrant.host}}"
              port: "{{qdrant.port}}"
              apiKey: "{{qdrant.api.key}}"
