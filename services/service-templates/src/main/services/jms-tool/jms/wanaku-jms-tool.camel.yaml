- route:
    id: route-3543
    from:
      uri: ai-tool:send-message-using-jms
      parameters:
        tags: jms
        description: "Send messages using JMS"
        parameter.wanaku_body: string
        parameter.wanaku_body.description: "Send messages using JMS"
      steps:
        - log: ${body}
        - to:
            id: to-2964
            uri: jms
            parameters:
              destinationType: queue
              destinationName: "{{request.queue}}"
              replyTo: "{{response.queue}}"
              connectionFactory: "#connectionFactory"
