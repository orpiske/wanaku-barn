Review all test plans under `tests/plans/` for any coverage that tests or references the old pre-0.1.0 CIC arguments (`--dependencies`, `--routes-ref`, `--rules-ref`, `--dependenciesRef`, `--routesRef`, `--rulesRef`, `ROUTES_PATH`, `ROUTES_RULES`). Since 0.1.0, Wanaku uses service catalogs instead of individually pointing specific elements via these flags. Remove any test steps, assertions, or descriptions that validate the old `--*refs` behavior. Also review the `operator-camel-route.md` and `camel-integration-capability.md` plans for any implicit assumptions that a CIC container receives these arguments (e.g., verifying environment variables like `ROUTES_PATH` or `ROUTES_RULES` on CIC pods). Update the plans to reflect the current service-catalog-based architecture. Reference issue #1728 in commit messages. Close #1728 if the test plans are already clean and no changes are needed — comment on the issue explaining the finding.
